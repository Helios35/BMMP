/**
 * The page-level layout primitives — one answer, for every route, to the
 * questions `UX_SPEC.md` §1.4 leaves to the builder.
 *
 * Extracted from the twelve screens unit 01 built rather than invented for them:
 * every primitive here is used by two or more routes, and anything that would
 * have been used once was left where it was (`PROJECT_SETUP_BMMP.md` §3.3).
 */

export * from "./page-layout";
export * from "./leading-icon";
export * from "./page-section";
export * from "./empty-state";
