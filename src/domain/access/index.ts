/**
 * The role→route capability map and the vocabulary it is expressed in.
 *
 * `SITE_ARCHITECTURE.md` §5.2 and §7.2 — one map, one place, read by the guard,
 * the navigation, the command palette and every cross-route link.
 */

export * from "./capability";
export * from "./routes";
export * from "./route-capability";
