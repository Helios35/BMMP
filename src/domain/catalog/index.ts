/**
 * Catalog matching as pure functions — `TECHNICAL_SPEC.md` §11.1 step 4;
 * Rules 2.18, 2.19.
 *
 * It ranks candidates and says whether exactly one resolved. **It never
 * selects one**: a human picks from the list, always.
 */

export * from "./match";
